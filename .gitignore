(product/security.html and product/updates.html are HTML source files, not build artifacts or temporary files, so no additional ignores are needed beyond what's already in the .gitignore if anything)

(empty)